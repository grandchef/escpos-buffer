export enum Align {
  Left,
  Center,
  Right,
}

export enum Cut {
  Full,
  Partial,
}

export enum Drawer {
  First,
  Second,
}

export enum Style {
  Bold = 1,
  Italic = 2,
  Underline = 4,
  Condensed = 8,
  DoubleWidth = 16,
  DoubleHeight = 32,
}
